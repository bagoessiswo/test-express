const express = require('express')
const router = express.Router()
// const workDayCron = require('../../cron/workdays')
// const tripCron = require('../../cron/trip')
// const targetCron = require('../../cron/target')
// router.use(workDayCron.init())
// router.use(workDayCron.closing())
// router.use(tripCron.closing())
// router.use(tripCron.calculateDistance())
// router.use(tripCron.calculateVisitationDistance())
// router.use(targetCron.weeklyTarget())

module.exports = router
